; EXPECT: ensure_runtime_builtins injects builtin declarations
target triple = "unknown-unknown-unknown"
target datalayout = ""

%BuiltinStr = type { ptr, i32 }
%BuiltinString = type { ptr, i32, i32 }

declare void @print(%BuiltinStr)

declare void @println(%BuiltinStr)

declare void @exit(i32)

declare %BuiltinString @String_from(%BuiltinStr)

declare i32 @Array_len(ptr)

