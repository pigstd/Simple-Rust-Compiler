// Test array repeat initialization and summation
// Generator: Codex
fn main() {
    let arr: [i32; 4] = [7; 4];
    let mut i: i32 = 0;
    let mut sum: i32 = 0;
    while (i < 4) {
        sum = sum + arr[i];
        i = i + 1;
    }
    exit(sum - 28);
}
